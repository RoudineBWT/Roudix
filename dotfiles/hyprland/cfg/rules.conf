# ──────────────────────────────────────
# WINDOW RULES
# ──────────────────────────────────────

# ─── Discord → ws 4 (Chat), fixed size, floating ───
windowrulev2 = workspace 4,    class:^(discord)$
windowrulev2 = float,          class:^(discord)$
windowrulev2 = size 1316 1011, class:^(discord)$

# ─── Element → ws 4, fixed size, floating ───
windowrulev2 = workspace 4,    class:^(Element)$
windowrulev2 = float,          class:^(Element)$
windowrulev2 = size 1316 1011, class:^(Element)$

# ─── Telegram → ws 4, fixed size, top-right corner ───
windowrulev2 = workspace 4,     class:^(org.telegram.desktop)$
windowrulev2 = float,           class:^(org.telegram.desktop)$
windowrulev2 = size 555 1011,   class:^(org.telegram.desktop)$
windowrulev2 = move 100%-555 0, class:^(org.telegram.desktop)$

# ─── Ghostty → ws 3 (Terminal), floating, fixed size ───
windowrulev2 = workspace 3,    class:^(com.mitchellh.ghostty)$
windowrulev2 = float,          class:^(com.mitchellh.ghostty)$
windowrulev2 = size 1505 755,  class:^(com.mitchellh.ghostty)$
windowrulev2 = center,         class:^(com.mitchellh.ghostty)$

# ─── Kitty → ws 3, floating ───
windowrulev2 = workspace 3,    class:^(kitty)$
windowrulev2 = float,          class:^(kitty)$

# ─── Ptyxis → ws 3 ───
windowrulev2 = workspace 3,    class:^(org.gnome.Ptyxis)$

# ─── Zen Browser → ws 1 (Browser), maximized ───
windowrulev2 = workspace 1,    class:^(zen-twilight)$
windowrulev2 = maximize,       class:^(zen-twilight)$

# ─── Zen PiP → floating, pinned on top ───
windowrulev2 = float,          class:^(zen-twilight)$, title:^(Picture-in-Picture)$
windowrulev2 = pin,            class:^(zen-twilight)$, title:^(Picture-in-Picture)$

# ─── Zen About dialog → floating ───
windowrulev2 = float,          class:^(zen-twilight)$, title:^(About Zen Twilight)$

# ─── Firefox → ws 1, maximized ───
windowrulev2 = workspace 1,    class:^(firefox)$
windowrulev2 = maximize,       class:^(firefox)$

# ─── Firefox PiP → floating, pinned on top ───
windowrulev2 = float,          class:^(firefox)$, title:^(Picture-in-Picture)$
windowrulev2 = pin,            class:^(firefox)$, title:^(Picture-in-Picture)$

# ─── Firefox About dialog → floating ───
windowrulev2 = float,          class:^(firefox)$, title:^(About Mozilla Firefox)$

# ─── Brave → ws 2 (Code), maximized ───
windowrulev2 = workspace 2,    class:^(brave-browser)$
windowrulev2 = maximize,       class:^(brave-browser)$

# ─── Zed → ws 2, maximized ───
windowrulev2 = workspace 2,    class:^(dev.zed.Zed)$
windowrulev2 = maximize,       class:^(dev.zed.Zed)$

# ─── Steam → ws 5 (Gaming), maximized ───
windowrulev2 = workspace 5,    class:^(steam)$
windowrulev2 = maximize,       class:^(steam)$

# ─── Steam notification toasts → floating, bottom-right corner ───
windowrulev2 = float,                      class:^(steam)$, title:^(notificationtoasts_.*)$
windowrulev2 = move 100%-320 100%-120,     class:^(steam)$, title:^(notificationtoasts_.*)$

# ─── Steam Friends List → ws 5, floating ───
windowrulev2 = workspace 5,    title:^(Friends List)$
windowrulev2 = float,          title:^(Friends List)$

# ─── Steam games → ws 5, fullscreen ───
windowrulev2 = workspace 5,    class:^(steam_app_.*)$
windowrulev2 = fullscreen,     class:^(steam_app_.*)$

# ─── Heroic → ws 5, fullscreen ───
windowrulev2 = workspace 5,    class:^(heroic)$
windowrulev2 = fullscreen,     class:^(heroic)$

# ─── Prism Launcher → ws 5, maximized ───
windowrulev2 = workspace 5,    class:^(org.prismlauncher.PrismLauncher)$
windowrulev2 = maximize,       class:^(org.prismlauncher.PrismLauncher)$

# ─── Minecraft → ws 5, fullscreen ───
windowrulev2 = workspace 5,    class:^(Minecraft.*)$
windowrulev2 = fullscreen,     class:^(Minecraft.*)$

# ─── OpenRGB → ws 5 ───
windowrulev2 = workspace 5,    class:^(openrgb)$

# ─── Nautilus → ws 6 (Files), maximized ───
windowrulev2 = workspace 6,    class:^(org.gnome.Nautilus)$
windowrulev2 = maximize,       class:^(org.gnome.Nautilus)$

# ─── Nautilus file dialogs → floating (Save As, Open...) ───
windowrulev2 = float,          class:^(org.gnome.Nautilus)$, title:^(Save As)$
windowrulev2 = float,          class:^(org.gnome.Nautilus)$, title:^(Open)$

# ─── Text Editor → ws 6 ───
windowrulev2 = workspace 6,    class:^(org.gnome.TextEditor)$

# ─── Spotify → ws 7 (Music), maximized ───
windowrulev2 = workspace 7,    class:^(spotify)$
windowrulev2 = maximize,       class:^(spotify)$

# ─── EasyEffects → ws 7 ───
windowrulev2 = workspace 7,    class:^(com.github.wwmm.easyeffects)$
