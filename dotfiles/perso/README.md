# Personal overrides

This directory is intentionally ignored by Git.

You can put your personal configuration files here to override the system-wide
dotfiles (e.g. for NixOS or Home Manager).

Nothing in this folder will be committed.

Typical use cases:
- machine-specific settings
- secrets or private configs
- experimental overrides

Just drop your files here and they will take precedence over the default config.
